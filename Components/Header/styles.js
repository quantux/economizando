import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    header: {
        color: '#fff',
        flexDirection: 'row',
        paddingTop: 25,
        paddingBottom: 25,
        width: '100%',
        paddingLeft: 10,
    },

    header_img: {
        width: 30,
        height: 30,
    },

    header_txt: {
        color: '#fff',
        fontSize: 20,
        marginLeft: 10,
    }
});

export default styles;